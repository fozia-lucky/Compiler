
#include <stdbool.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>



int main()
{

    char ch[100] = "{hello + world}/universe ";

     if (ch == '+' || ch == '-' || ch == '*' ||
        ch == '/' || ch == '>' || ch == '<' ||
        ch == '=')

printf("hyj",ch);


    return (0);
}
